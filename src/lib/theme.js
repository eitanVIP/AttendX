// Mirrors the --accent default in index.css. Lives here for the places that
// need a concrete value rather than the CSS variable: the Settings colour
// picker, and the colorPrimary stored on a brand-new team.
export const DEFAULT_ACCENT = '#2563eb'
